//
// Created by jaeseong on 23. 6. 8.
//
#include "StreamTemplate.h"

vector<char> doori::Stream::StreamTemplate::MakeStream() const {
    mHeader.

    return nullptr;
}

doori::Stream::StreamTemplate::StreamTemplate(doori::Stream::IHeader &header, doori::Stream::IBody &body) : Common::Error(0, true), mHeader{header}, mBody{body}{

}
